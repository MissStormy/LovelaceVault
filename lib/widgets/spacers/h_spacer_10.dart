import 'package:flutter/material.dart';

class HSpacer10 extends StatelessWidget {
  const HSpacer10({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 10.0,
    );
  }
}